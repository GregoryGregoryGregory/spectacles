version: '3'
services:
  web:
    build: .
    depends_on:
      - redis
    environment:
      - VIRTUAL_HOST=localhost
  nginx:
    image: jwilder/nginx-proxy
    ports:
      - 80:80
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
  redis:
    image: redis
